uaseradd -m -G wheel evrosvet
passwd evrosvet
echo 'evrosvet ALL=(ALL) SETENV: ALL' >> /etc/sudoers
export PKG_PATH=ftp://ftp.openbsd.org/pub/OpenBSD/$(uname -r)/packages/$(uname -m)/
sudo pkg_add -v bash vim screen elinks mc nano sudo

$ sudo usermod -s /usr/local/bin/bash
$ echo 'export EDITOR=nano' >> ~/.bashrc
$ echo 'export PKG_PATH=ftp://ftp.openbsd.org/pub/OpenBSD/$(uname -r)/packages/$(uname -m)/' >> ~/.bashrc
$ bash

sudo pkg_add -v xfce4-session xfdesktop xfwm4


$ echo 'LC_CTYPE="ru_RU.UTF-8"' > ~/.xinitrc
$ echo 'setxkbmap "us,ru" ",winkeys" "grp:caps_toggle" &' \
>> ~/.xinitrc
$ echo 'exec xfce4-session' >> ~/.xinitrc
$ ln -s ~/.xinitrc ~/.xsession
$ startx

gtk-xfce-engine libxfce4mcs libxfcegui4 xfce-mcs-manager xfce-mcs-plugins xfce-utils xfce4-* xfdesktop xfwm4 xfwm4-themes
=> in file
sudo pkg_add -v $(cat файл)

$ sudo pkg_add -v slim


=> with login
nano /etc/rc.local
=>
[ -x /etc/rc.d/slim ] && /etc/rc.d/slim start

=>autologin
nano /etc/slim.conf

default_user evrosvet
auto_login yes

https://xakep.ru/2012/12/12/dekstop-open-bsd/
